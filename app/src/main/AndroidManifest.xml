<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ce.yildiz.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="true">

        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.login.SignUpActivity" />

        <activity android:name=".ui.login.ForgotPasswordActivity" />

        <activity android:name=".ui.login.PasswordResetActivity" />

        <activity android:name=".ui.email.EmailComposeActivity" />

        <activity android:name=".ui.user.userlist.UserListActivity" />

        <activity android:name=".ui.user.UserDetailActivity" />

        <activity android:name=".ui.navigation.NavigationActivity" />

        <activity android:name=".ui.notes.NoteEditActivity" />

        <activity android:name=".ui.notes.notelist.NoteListActivity" />

        <activity android:name=".ui.sensor.SensorActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>